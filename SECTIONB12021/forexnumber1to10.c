#include<stdio.h>
void main(void)
{
int a;
for(a=1;a<=10;a=a+1)
{
printf("%d\n",a);
}
}
//Keywords are reserved words with a special meaning. In C   there are around 32 keywords.
//E.g.: void , int, for
//In for loop there are three parts - initialization (a=1) , condition (a<=10), operation (a=a+1).
